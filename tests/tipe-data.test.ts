describe("Data Type", function () {
  it("should must declare", function () {
    const name: string = "Gustio Nusamba";
    const age: number = 22;
    const isMarried: boolean = false;

    console.info(name);
    console.info(age);
    console.info(isMarried);
  });
});
